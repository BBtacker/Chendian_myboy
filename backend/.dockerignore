# 排除在 Docker 构建上下文之外，避免把无关大文件传进镜像
# Go 服务镜像只需源码与 go.mod，不需要 Python 虚拟环境、前端依赖等

# Python 特征提取服务的虚拟环境（含 torch，约 3.7GB）——镜像内会重新 pip 安装
feature_extractor/.venv/
feature_extractor/__pycache__/
feature_extractor/models/
feature_extractor/export_model.py

# 前端（不在 backend 构建范围内）
front/

# 构建产物 / 缓存
target/
dist/
bin/
*.exe
*.test

# 版本控制 / IDE / 环境
.git/
.gitignore
.idea/
.vscode/
.workbuddy/
.trae/

# 系统临时文件
**/.DS_Store
*.log
